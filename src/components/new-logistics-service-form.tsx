"use client";

import Link from "next/link";
import { useActionState, useState } from "react";
import { createLogisticsService, type LogisticsActionState } from "@/app/actions/logistics";
import { money } from "@/lib/accounting";

const initialState: LogisticsActionState = {};

export function NewLogisticsServiceForm() {
  const [state, formAction, isPending] = useActionState(createLogisticsService, initialState);
  const [cost, setCost] = useState("");
  const [rate, setRate] = useState("0.10");
  const [advance, setAdvance] = useState("0");
  const [fee, setFee] = useState("0.41");
  const originalCost = Number(cost) || 0;
  const profitability = originalCost * (Number(rate) || 0);
  const providerPayment = originalCost - profitability - (Number(advance) || 0);
  const transferred = providerPayment - (Number(fee) || 0);
  const amount = (name: string, label: string, value: string, setValue: (value: string) => void, required = true) => <label className="text-sm font-medium">{label}<input required={required} name={name} type="number" min="0" step="0.01" value={value} onChange={(event) => setValue(event.target.value)} className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label>;
  return <form action={formAction} className="mt-7 grid gap-6 xl:grid-cols-[1fr_280px]"><section className="rounded-xl border border-[var(--line)] bg-white p-6"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">Fecha del servicio<input required name="service_date" type="date" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label><label className="text-sm font-medium">N.º de servicio<input name="service_number" placeholder="Ej. SL-001" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label><label className="text-sm font-medium sm:col-span-2">Servicio / detalle<input required name="description" placeholder="Ej. Transporte de mercadería" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label><label className="text-sm font-medium">Proveedor / chofer<input required name="provider_name" placeholder="Nombre completo o empresa" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label><label className="text-sm font-medium">Accionista<input required name="shareholder" placeholder="Jorge, Daniel o Favio" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label><label className="text-sm font-medium">N.º de factura<input name="invoice_number" placeholder="FACTURA #160" className="mt-1.5 w-full rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label>{amount("original_cost", "Costo original (USD)", cost, setCost)}{amount("profitability_rate", "Porcentaje cobrado", rate, setRate)}{amount("advance", "Abono (USD)", advance, setAdvance, false)}{amount("transfer_fee", "Comisión de transferencia", fee, setFee, false)}<label className="text-sm font-medium">Estado<select name="status" defaultValue="Pendiente" className="mt-1.5 w-full rounded-lg border border-[var(--line)] bg-white px-3 py-2.5 text-sm"><option>Pendiente</option><option>Facturada</option><option>Pagada</option></select></label></div><label className="mt-5 block text-sm font-medium">Observación <span className="font-normal text-[var(--muted)]">(opcional)</span><textarea name="notes" rows={3} className="mt-1.5 w-full resize-none rounded-lg border border-[var(--line)] px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]" /></label>{state.error && <p role="alert" className="mt-5 rounded-lg bg-[#fdefec] px-4 py-3 text-sm text-[#963f32]">{state.error}</p>}<div className="mt-7 flex justify-end gap-3"><Link href="/logistica" className="rounded-lg px-4 py-2.5 text-sm font-medium">Cancelar</Link><button disabled={isPending} className="rounded-lg bg-[var(--accent)] px-4 py-2.5 text-sm font-medium text-white disabled:opacity-60">{isPending ? "Guardando…" : "Guardar servicio"}</button></div></section><aside className="h-fit rounded-xl bg-[#173e30] p-5 text-white"><p className="text-sm font-medium text-[#b8dac8]">Vista previa</p><h2 className="mt-1 text-lg font-semibold">Reserva de saldo</h2><p className="mt-4 text-sm leading-6 text-[#d5e8dd]">Al guardar, se descuenta automáticamente el pago al proveedor del saldo del accionista.</p><dl className="mt-5 space-y-4 text-sm"><div className="border-b border-white/15 pb-3"><dt className="text-[#b8dac8]">Rentabilidad</dt><dd className="mt-1 font-mono text-lg">{money.format(profitability)}</dd></div><div className="border-b border-white/15 pb-3"><dt className="text-[#b8dac8]">Pago al proveedor</dt><dd className="mt-1 font-mono text-lg">{money.format(providerPayment)}</dd></div><div><dt className="text-[#b8dac8]">Monto transferido</dt><dd className="mt-1 font-mono text-lg">{money.format(transferred)}</dd></div></dl></aside></form>;
}
