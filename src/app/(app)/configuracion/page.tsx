import { UsersRound } from "lucide-react";

export default function SettingsPage() { return <div className="mx-auto max-w-5xl px-5 py-8 sm:px-8"><p className="mb-1 text-sm font-medium text-[var(--accent)]">Administración</p><h1 className="text-3xl font-semibold tracking-tight">Configuración</h1><section className="mt-7 rounded-xl border border-[var(--line)] bg-white p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-lg bg-[var(--accent-soft)] text-[var(--accent)]"><UsersRound size={19}/></span><div><h2 className="font-semibold">Equipo con acceso</h2><p className="text-sm text-[var(--muted)]">Los tres integrantes tienen el mismo nivel de acceso operativo.</p></div></div><p className="mt-5 rounded-lg bg-[#f5f7f5] p-4 text-sm text-[var(--muted)]">Cree los tres usuarios en Supabase Authentication y agréguelos a <code className="font-mono text-xs">organization_members</code>. Las políticas RLS bloquean a usuarios externos.</p></section></div>; }
