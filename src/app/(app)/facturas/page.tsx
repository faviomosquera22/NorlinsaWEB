import { FileWarning } from "lucide-react";

export default async function InvoicesPage({ searchParams }: { searchParams: Promise<{ filtro?: string }> }) { const params = await searchParams; const expiring = params.filtro === "caducar"; return <div className="mx-auto max-w-5xl px-5 py-8 sm:px-8"><p className="mb-1 text-sm font-medium text-[var(--accent)]">Seguimiento</p><h1 className="text-3xl font-semibold tracking-tight">{expiring ? "Facturas por caducar" : "Facturas y pendientes"}</h1><p className="mt-2 text-sm text-[var(--muted)]">Las guías sin factura y las fechas de vencimiento se consolidan aquí.</p><section className="mt-7 rounded-xl border border-[var(--line)] bg-white p-8 text-center"><FileWarning className="mx-auto text-[var(--accent)]" size={28}/><h2 className="mt-4 font-semibold">Sin alertas configuradas</h2><p className="mx-auto mt-2 max-w-md text-sm text-[var(--muted)]">Al importar el archivo, el sistema identificará facturas pendientes y permitirá definir el plazo de caducidad por documento.</p></section></div>; }
