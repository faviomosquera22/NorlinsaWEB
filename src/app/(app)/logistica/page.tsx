import { Plus, Truck } from "lucide-react";
import Link from "next/link";
import { money } from "@/lib/accounting";
import { getLogisticsServices } from "@/lib/logistics";

export default async function LogisticsPage() { const { services, isDemo } = await getLogisticsServices(); return <div className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="mb-1 text-sm font-medium text-[var(--accent)]">Operación</p><h1 className="text-3xl font-semibold tracking-tight">Servicios logísticos</h1><p className="mt-2 text-sm text-[var(--muted)]">Servicios que reservan el saldo disponible del accionista al registrarse.</p></div><Link href="/logistica/nueva" className="inline-flex items-center gap-2 rounded-lg bg-[var(--accent)] px-4 py-2.5 text-sm font-medium text-white"><Plus size={17}/>Nuevo servicio</Link></header>{isDemo && <p className="mt-6 rounded-lg bg-[#fff7df] px-4 py-3 text-sm text-[#765c1c]">Vista de ejemplo. Los datos reales aparecerán después de ejecutar la migración de servicios logísticos.</p>}<section className="mt-6 overflow-hidden rounded-xl border border-[var(--line)] bg-white"><div className="border-b border-[var(--line)] p-5"><h2 className="font-semibold">Servicios registrados</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-[#fafbfa] text-xs uppercase tracking-wide text-[var(--muted)]"><tr><th className="px-5 py-3">Fecha</th><th className="px-5 py-3">Servicio</th><th className="px-5 py-3">Proveedor</th><th className="px-5 py-3">Accionista</th><th className="px-5 py-3">Costo</th><th className="px-5 py-3">Reserva</th><th className="px-5 py-3">Estado</th></tr></thead><tbody>{services.map((service) => <tr key={service.id} className="border-t border-[var(--line)]"><td className="px-5 py-4 text-[var(--muted)]">{service.date}</td><td className="px-5 py-4"><strong className="block font-medium">{service.number}</strong><span className="text-xs text-[var(--muted)]">{service.description}</span></td><td className="px-5 py-4">{service.provider}</td><td className="px-5 py-4">{service.shareholder}</td><td className="px-5 py-4 font-mono text-xs">{money.format(service.cost)}</td><td className="px-5 py-4 font-mono text-xs text-[#b04a39]">−{money.format(service.providerPayment)}</td><td className="px-5 py-4"><span className="rounded-full bg-[#edf4ef] px-2.5 py-1 text-xs font-medium text-[#356247]">{service.status}</span></td></tr>)}</tbody></table></div>{services.length === 0 && <div className="p-10 text-center text-sm text-[var(--muted)]"><Truck className="mx-auto mb-3 text-[var(--accent)]"/>No hay servicios logísticos registrados.</div>}</section></div>; }
